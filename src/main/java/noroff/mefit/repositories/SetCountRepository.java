package noroff.mefit.repositories;

import noroff.mefit.models.SetCount;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SetCountRepository extends JpaRepository<SetCount, Integer> {
}
